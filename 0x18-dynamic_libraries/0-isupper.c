#include "main.h"

/**
 * _isupper - checks if the letter is uppercase
 * @c: char to be checked
 * Return: 1 if true, 0 not true
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);

	return (0);
#include "main.h"

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);

	return (0);
}
